# 意图识别演进史

> 把分散在 commit message 与对话里的设计调整，集中归档成一条演进链。
> **起草日期**：2026-07-01
> **关联**：`INTENT_DOMAIN_REWORK_PLAN.md`（领域分流，上游）、`SMART_CLARIFY_TOOLGEN_PLAN.md`（开发计划）、`INTENT_RECOGNITION_REPORT.md`（设计+实测复盘）
> **分支**：`feat/smart-clarify-toolgen`

---

## 1. 一句话演进

从 **「机械查表」**（旧解析器抽类型 ID + `route_decision` 按 ID 有无选预设追问模板）演进到 **「通用常识推理」**（`IntentAgent` 一次判够不够 / 问什么 / 缺工具则现生成）。

核心驱动力一句话：**用户要求意图识别体现智能，而不是机械查类型表。**

---

## 2. 两个阶段

### 阶段一：领域分流（T1–T7，2026-06-23）— 意图识别的**上游前提**

先解决「这句话是不是生信任务 + 归哪个脚本库」。意图识别（库内判够不够）只有在这层做对之后才有意义。

| commit | 时间 | 任务 |
|---|---|---|
| `f702c38` | 06-23 01:58 | T1 导入库时 LLM 自动生成领域描述/关键词 |
| `265e6ce` | 06-23 02:26 | T3 有效性闸 + 修单库短路（不再无条件钦定单库） |
| `858c3bf` | 06-23 02:35 | T4 few-shot + 多库候选消歧 |
| `d3d231d` | 06-23 02:39 | T5 早期重判领域 + 路径确认后锁定 |
| `5b7eff0` | 06-23 02:41 | T6 低置信解析先追问再规划 |
| `f034a1e` | 06-23 02:43 | T7 收拢有效性逻辑到 T3 闸 |
| `315c35d` | 06-23 17:08 | 新增 stats 域（多库场景可测） |
| `0255a6d` | 06-23 17:10 | 域感知追问模板 + 原始数据→全流程路由修复 |

**产物**：`DomainClassifier`（相关性闸 + 多库消歧）、`INTENT_DOMAIN_REWORK_PLAN.md`。

### 阶段二：库内意图识别（IntentAgent，06-23 → 07-01）— **本体**

领域已定后，判断「信息够不够、该问什么、够但缺工具怎么办」。这是本文的主线。

---

## 3. 意图识别本体的三次关键设计调整

> 这三次调整是用户在开发中明确拍板的，方向一旦定了就贯穿后续所有实现。

### 调整 ①：预设具体工具 → 通用常识驱动
- **commit**：`0753509`（06-23 22:45）
- **改前**：初版计划（`a0b9b2d`）预设「单端 → 生成 `amp-dada2-se`」，把缺口映射到固定脚本。
- **改后**：不预设。LLM 在运行时按通用生信常识决定生成什么工具，单端只是触发此机制的例子之一。
- **动因（用户原话精炼）**：「pcoa 分析只是个例子……我希望系统能从生物信息分析的一般常识角度去自动追问」「LLM 现写的脚本不应是预设的」。

### 调整 ②：保留旧 parser → IntentAgent 取代（Option 1）
- **commit**：`1476f30`（计划，06-26 01:21）+ `85029e0`（实现，Phase 1）
- **改前**：`ParserService`（LLM 抽类型 ID + `confidence`）+ `_route_decision` 按 available/goal 有无选预设追问（CASE A/B/C/D）——本质是关键词/类型菜单驱动。
- **改后**：`IntentAgent` 一次 LLM 常识推理，输出 `{sufficient, questions, available_inputs, goal_types, reasoning}`；删掉所有预设追问分支，类型词表降级为「够意图后的翻译目标」。
- **动因（用户原话）**：「我要方案 1……我非常需要在意图识别中体现智能，而不是机械的查此表」。

### 调整 ③：单问题 → 一次问全
- **commit**：`1476f30`（06-26 01:21，与 ②同批）
- **改前**：追问只问一个问题（挤牙膏式多轮）。
- **改后**：`questions[]` 一次抛出所有「不定就跑偏」的关键问题（数据形态 / 单双端 / 引物 / 目标具体化）。
- **动因（用户原话）**：「也不止是最该问的那一个问题，而是可以问多个问题，一次性都问了，免得过多轮次分散用户的重点意图」。

---

## 4. 能力补齐：从「识别意图」到「补缺口」（Phase 2 / 3）

意图识别不再止步于「够不够」，而是延伸到「够但平台缺工具」的现生成闭环。

### Phase 2：能力缺口识别（`a3097c1`，06-26 01:53）
- 规划器找不到路 → `classify_capability_gap` 用常识判「合理任务、只是缺工具」→ 提议「这块我本来干不了，但我可以现生成一个单步脚本，要吗？」
- 同批「端到端可靠性修复」（开发中追加，计划里没有）：
  - **原始数据 → amplicon 安全网**：LLM 在这条上不可靠（会把「原始数据+PCoA」误判到只做下游的 stats，还谎称"未提原始数据"），在 LLM 前加硬编码安全网。
  - **dup-msg 修复**：history 含当前消息导致「两条连续 user 消息」，LLM 回裸消息 → 排除当前消息。
  - **temperature=0**：稳定 sufficient 判断。

### Phase 3：现生成 + 注册闭环（`78494bb`，07-01 13:24）
- 用户答「好」→ 检测 pending offer → LLM 判 accept → `ToolGenesisService` 生成单步工具（.sh + 契约）→ 串链校验 → 落盘 + 写 `Script` + `invalidate` → 重规划出含新工具的路径。
- **开发中追加的关键约束**（计划里没有，实测暴露的）：注册后**路径校验**——首版 LLM 只输出 `FEATURE_TABLE` 单类型，导致 taxonomy/phylogeny 链断、到不了 PCoA；串链不变式（output 被消费）通过了但「到达目标」没保证。于是加 ① prompt 强制输出完整（开启到目标的下游链路）② 注册后 `plan_workflow` 验证能到 goal，否则清理拒收、不留残缺工具。

---

## 5. 踩过的坑（教训备忘）

| 坑 | 现象 | 根因 | 处置 |
|---|---|---|---|
| **curl 编码红鲱鱼** | Phase 1 调试期，IntentAgent 对中文输入看似失效，反复改 `SYSTEM_PROMPT` 4 版都不行 | Windows curl 把中文转成 `?`，LLM 实际收到 `?????PCoA`——**测试工具问题，不是系统问题** | 改用 httpx（标准 UTF-8，与前端 axios 一致）；记入记忆「Windows 上别用 curl 测中文 API」。prompt 膨胀可瘦身但能工作，未做 |
| **LLM 域路由不可靠** | 「原始数据+PCoA」被误判到 stats | LLM 在「方法名相同按数据类型区分」上不稳 | 硬编码安全网（见 Phase 2） |
| **生成工具断链** | `amp-dada2-se` 注册后规划仍为 0 路径 | LLM 只输出单个中间产物类型 | 输出完整性 prompt + 注册后路径校验（见 Phase 3） |

---

## 6. 当前状态（截至 2026-07-01）

- **IntentAgent**：一次常识推理，`sufficient` / `questions[]`（一次问全）/ 类型 ID（翻译用），不查工具表。
- **能力缺口**：规划空 → 判「合理但缺工具」→ 提议现生成。
- **现生成**：`ToolGenesisService` 生成 + 串链校验 + 注册后路径校验 + 自动入库。
- **实测**：5 种真实客户口吻问法（规整 / 大白话 / 行话 / 不完整）均给出专业追问，单端→提议→同意→生成 7 步路径端到端跑通（详见 `INTENT_RECOGNITION_REPORT.md`）。

---

## 7. 文档索引

| 文档 | 定位 | 日期 |
|---|---|---|
| `INTENT_DOMAIN_REWORK_PLAN.md` | 领域分流 T1-T7（上游） | 06-23（游离，未入 git） |
| `SMART_CLARIFY_TOOLGEN_PLAN.md` | 智能追问+现生成**开发计划** | 06-23 ~ 06-26 |
| `INTENT_RECOGNITION_REPORT.md` | 设计复盘 + 实测 | 07-01 |
| **本文 `INTENT_RECOGNITION_EVOLUTION.md`** | **演进史（本文）** | 07-01 |

---

## 8. 演进链速查

```
旧: ParserService 抽类型ID + route_decision 预设追问（CASE A/B/C/D）
      │
      │  ① 预设工具 → 通用常识驱动      (0753509, 06-23)
      │  ② 保留 parser → IntentAgent 取代 (1476f30/85029e0, 06-26)
      │  ③ 单问题 → 一次问全             (1476f30, 06-26)
      ▼
新: IntentAgent 一次常识推理 → 够则规划 / 不够则一次问全
      │
      │  Phase 2 规划空 → 能力缺口提议   (a3097c1, 06-26)
      │  Phase 3 同意 → 现生成+注册+重规划 (78494bb, 07-01)
      ▼
闭环: 单端PCoA → 提议 → "好" → 生成 amp-dada2-se → 7步路径直达 PCoA
```
