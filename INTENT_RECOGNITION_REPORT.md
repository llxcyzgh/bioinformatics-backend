# 意图识别设计与实测报告

> **分支**：`feat/smart-clarify-toolgen`
> **实测日期**：2026-07-01
> **模型**：qwen-plus（DashScope，temperature=0）
> **相关代码**：`app/services/intent_agent.py`、`app/services/chat_service.py`、`app/services/tool_genesis_service.py`
> **关联文档**：`SMART_CLARIFY_TOOLGEN_PLAN.md`（Phase 1 意图代理设计）

---

## 1. 结论

意图识别（判断信息够不够、该问什么）**基于通用生物信息分析常识**，不基于平台现有工具库。平台类型词表只在"判定意图已足够"之后充当**翻译目标**（把意图映射成可规划的类型 ID）。`IntentAgent` 全程**不读取工具（`Script`）表**。

这是有意做的隔离：让"理解用户"和"平台有什么工具"两个问题解耦。

---

## 2. 设计：三层结构

`IntentAgent.understand()` 一次 LLM 调用，喂给模型三样东西：

- `Domain.description` —— 该领域能做什么的**大白话描述**（常识背景）
- `DataType` 类型词表 —— 该领域可识别的数据类型（`type_id` + `label`）
- 用户原话 + 对话历史

输出 `{sufficient, questions[], available_inputs[], goal_types[], reasoning}`。

### 2.1 理解层（sufficient + questions）— 纯常识

`SYSTEM_PROMPT` 第一条硬约束（`intent_agent.py`）：

> 用户明确声明拥有的数据就是唯一入口……**严禁追问"你有没有更下游的处理产物（丰度表 / 距离矩阵 / 已去噪的 ASV 等）"**……只要用户已说明【数据形态】+【分析目标】，就 `sufficient=true` 直接抽取，**不要因为"从原始数据到目标还要好几步"而降级或追问**。下游能否实现由别的模块判断。

判定"够不够"靠的是生信常识本身（如"单/双端决定后续流程"才问单双端），**不是**"平台有没有单端工具"。`reasoning` 字段即模型给出的常识依据；LLM 不可用时兜底为通用追问"你有什么数据 / 想要什么结果"，绝不替用户默认数据形态。

### 2.2 翻译层（available_inputs / goal_types）— 平台词表

判定 `sufficient=true` 后，才把意图翻成类型 ID。这步受 `DataType` 词表约束：词表是**数据类型概念**（双端原始数据、PCoA 图、距离矩阵…），**不是工具清单**。词表外的类型会被 `_normalize()` 丢弃并记 warning，避免空规划。

### 2.3 工具表（Script）— 完全不碰

`IntentAgent.understand()` 只读 `Domain.description` + `DataType` 词表 + 用户话。它**不知道**平台装了 cutadapt/dada2/…。工具图（`Script` 表）要到后续 `PlannerService`（规划）和 `ToolGenesisService`（现生成）才第一次读。

| 层 | 数据来源 | 是否依赖工具库 |
|---|---|---|
| 理解（够不够 / 问什么） | 生信常识 + 领域能力描述 | 否 |
| 翻译（抽类型 ID） | `DataType` 类型词表 | 否（词表是类型概念，非工具） |
| 规划 / 生成 | `Script` 工具图 | 是（这里才第一次读） |

---

## 3. 为什么这样隔离——单端 case 是铁证

「我有单端原始数据，要做 PCoA」的处理链路：

```
IntentAgent      sufficient=true（单端+PCoA 是合理任务）   ← 它不知道平台没有单端工具
   ↓
PlannerService   在工具图里找不到路径（这时候才第一次发现"缺工具"）
   ↓
classify_capability_gap   判定"合理但缺工具"
   ↓
提议现生成 → 用户同意 → ToolGenesisService 生成 amp-dada2-se → 重规划出 7 步路径
```

**这条链能成立，恰恰因为 IntentAgent 不知道平台缺单端工具。** 若它查了工具表，"单端做 PCoA"在意图层就会被判死（无解/拒绝），永远不会走到"现生成"。意图识别与工具库解耦，才让"合理但做不了"的任务有机会被识别和补救。

---

## 4. 实测：真实客户口吻的问法

服务实跑（admin 账号，amplicon 域，qwen-plus），用 5 种**不规整**的问法探测 `IntentAgent` 实际反应（每条独立新任务）：

| # | 用户原话 | 响应 | 实际追问 / 方案 |
|---|---|---|---|
| 1 | 我有一堆测序数据，想看看样本之间差异大不大 | 追问 | ①单/双端？②用哪种距离/排序——**Bray-Curtis+PCoA、Unifrac+NMDS**？ |
| 2 | 老板让我分析下这批微生物数据，出个报告 | 追问 | ①数据是原始 FASTQ 还是已处理表？②单/双端？③想看哪些——**群落组成 / Alpha / Beta / LEfSe**？ |
| 3 | 帮我把这批 fastq 处理一下，看群落结构 | 追问 | ①单/双端？②"群落结构"具体指——**PCoA / NMDS / 热图 / 系统发育树**？ |
| 4 | 我有数据想做 alpha 和 beta 多样性 | 追问 | ①原始 FASTQ 还是丰度表？②单/双端？③要不要组间统计检验？ |
| 5 | 我有双端测序数据，想做物种注释和 PCoA | 直接出方案 | 10 步 workflow，`FASTQ_PAIR` → 物种注释 + PCoA |

### 解读

- **大白话接得住**：「样本差异大不大」「出个报告」「看群落结构」这类口语，系统没懵，还把模糊目标**翻译成专业可选项**（Bray-Curtis、LEfSe、系统发育树…）。
- **行话也接得住**：「alpha/beta 多样性」「fastq」被直接识别。
- **一次问全**：每条追问把所有"不定就跑偏"的关键缺口一次列完（数据形态 + 单双端 + 目标具体化），不挤牙膏。
- **不是模板匹配**：这些回应**不是关键词→预设问题映射**能产生的。「差异大不大 → Beta 多样性 → 给距离方法选项」这条链是 LLM 凭常识推理出来的；换一个没预设过的模糊问法（如「帮我看样本能不能分开」），它同样能给出恰当追问。

---

## 5. 边界与短板（诚实呈现）

| 短板 | 说明 | 性质 |
|---|---|---|
| **用户不知道"能问啥"** | 系统是被动追问，客户面对空对话框可能无从开口 | **最大缺口，但不属意图识别层——属 UX / 产品引导** |
| 目标超出领域 | 想做基因组拼接、转录组等 amplicon 不支持的分析 | 领域路由层友好降级 |
| 多意图混说 | 一次提多个目标（alpha+beta+LEfSe+热图），可能只抽部分 goal | prompt 引导抽全 / 前端确认 |
| 词表外概念 | 用户提的类型不在 `DataType` 词表（如"代谢组"），翻译抽不出 | 翻译层受限，非理解层 |

---

## 6. 建议（按收益排序）

1. **首屏引导（收益最大、改动最小）**：空对话时给示例问法 + 一句话说明能力，如「你可以这样问：我有双端 16S 数据，想做物种注释和多样性」。直接解决"不知道能问啥"。
2. **被动转主动**：无法理解时，追问同时简短告知「我能做扩增子全流程：从原始数据到多样性 / 差异分析」。
3. **超领域友好降级**：明确告知做不了 + 列出能做的。
4. **多意图抽全**：`SYSTEM_PROMPT` 加一条「把用户提到的所有分析目标都抽进 `goal_types`」。
5. **口语化 few-shot（优先级低）**：实测已够用，可选加强稳定性。

---

## 附录：实测方法

httpx 直连 `POST /api/chat/`（multipart form：`project_id` / `content` / `type`），每条用独立新任务，读取返回 `message.type`、`message.content`（追问文案）、`message.data` 中的 `available_inputs` / `goal_types`（翻译结果）。绕过 curl（Windows 下 curl 会把中文转成 `?`，曾导致长时间误调试；httpx 走标准 UTF-8，与前端 axios 一致）。
